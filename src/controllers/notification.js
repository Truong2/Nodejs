const Notification = require('../models/notification');

const query_notification = async (user_receive) =>{
  const list_notification = await Notification.find({

  })
}

const create_notification = async(user_receive, content, ) =>{
  
}